<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:vm="clr-namespace:Yodaz.ViewModel;assembly=Yodaz"
    xmlns:behaviors="clr-namespace:Yodaz.Behaviors"
    x:Class="Yodaz.View.StartPage"
    Title="Root Page">
    <ContentPage.BindingContext>
        <vm:StartViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
           <Label Text="Welcome to Yodaz trivia quiz"></Label>
           <Entry Placeholder="Enter number of questions" Text="{Binding Number}">
                <Entry.Behaviors>
                    <behaviors:NumberValidationBehavior/>
                    <behaviors:MaxLengthValidationBehavior MaxLength="30"/>
                </Entry.Behaviors>
            </Entry>
           <Button Text="Start quiz" Command="{Binding StartCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
